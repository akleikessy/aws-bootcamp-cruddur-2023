version: "3.9"
services:
  backend-flask:
    environment:
      FRONTEND_URL: "https://ominous-space-funicular-vpwxggvj97436g56-3000.app.github.dev"
      BACKEND_URL: "https://ominous-space-funicular-vpwxggvj97436g56-4567.app.github.dev"
    build: ./backend-flask
    ports:
      - "4567:4567"
    volumes:
      - ./backend-flask:/backend-flask
  frontend-react-js:
    environment:
      REACT_APP_BACKEND_URL: "https://ominous-space-funicular-vpwxggvj97436g56-4567.app.github.dev"
    build: ./frontend-react-js
    ports:
      - "3000:3000"
    volumes:
      - ./frontend-react-js:/frontend-react-js

# the name flag is a hack to change the default prepend folder
# name when outputting the image names
networks: 
  internal-network:
    driver: bridge
    name: cruddur